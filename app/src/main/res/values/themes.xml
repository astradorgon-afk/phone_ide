<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns all theming; this only prevents a flash of the platform default. -->
    <style name="Theme.MobileForge" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
